# QA Auditor v2 — 2026-05-19

**Agente**: qa-auditor (estático + DB + routing guards + business rules)
**Cobertura**: ~50% del checklist completo (no incluye browser E2E).
**Tiempo de ejecución**: ~3 min.

## Resumen

| Sección | Estado | Hallazgos |
|---|---|---|
| 1. Privacidad queries | ✅ PASS | 0 |
| 2. RLS habilitado | ✅ PASS | 0 |
| 3. RLS policies por tabla | ✅ PASS | 0 |
| 4. Storage policies path-based | ✅ PASS | 0 |
| 5. RPCs SECURITY DEFINER | ✅ PASS | 0 |
| 6. Schema integrity (icons/abbrev) | ✅ PASS | 0 |
| 7. Modalidad normalizada | ✅ PASS | 0 |
| 8. Validaciones db.* | ✅ PASS | 0 |
| 9. Patrones bug-prone | ✅ PASS | 0 |
| 10. Hex hardcoded | ✅ PASS | 0 (todos en excepciones legítimas) |
| 11. Migraciones sync | ⚠️ WARN | 1 |
| 12. Routing Guards | ✅ PASS | 0 |
| 13. Business Rules estáticas | ⚠️ WARN | 1 |

**Score**: 11/13 verde + 2 ⚠️ menores. **PASS general**.

---

## Detalle por sección

### 1. Privacidad de queries ✅

- `PROFILE_PUBLIC_COLS` definido en `supabase.js:35` — no incluye `email`, `birthday`, `birth_date`, `latitude`, `longitude`, `notification_prefs`.
- `COMPANY_PUBLIC_COLS` definido en `supabase.js:52` — no incluye `tax_id`, coords, prefs.
- `db.profiles.getPublicById` existe (`supabase.js:179`) y se usa correctamente en:
  - `Chat.jsx` (perfil del otro user en match)
  - `CompanyPublicProfileView.jsx` (candidato viendo empresa)
  - `CandidatePublicProfileView.jsx` (empresa viendo candidato)
- `db.profiles.getById` solo se usa en `AuthCallbackView`, `LoginView`, `ProfileView` (siempre para el propio user). ✅

### 2. RLS habilitado ✅

Query a `pg_class` filtrando `relrowsecurity = false`: **0 filas**. Todas las tablas de `public` tienen RLS habilitado.

### 3. RLS policies por tabla ✅

Tablas con >3 policies: solo `interviews` (5) y `user_statistics` (5). Las tablas críticas (companies, matches, messages, offers, profiles, swipes, notifications, support_tickets) están en 2-3 policies cada una (post-dedupe 011). ✅

### 4. Storage policies path-based ✅

Query a `pg_policies` buscando policies viejas permisivas (`Subida Permitida`, `Actualizacion Permitida`, `Borrado Permitido`, `Authenticated users can upload videos`, `tickets_insert_any`, `Service role can insert notifications`): **0 filas**. Todas eliminadas en migraciones 009/010. ✅

### 5. RPCs SECURITY DEFINER ✅

```
delete_account      | prosecdef=true  ✅
increment_stat      | prosecdef=true  ✅
log_daily_activity  | prosecdef=true  ✅
touch_updated_at    | prosecdef=false ← trigger function, no necesita SECURITY DEFINER ✅
```

### 6. Schema integrity ✅

NULLs en columnas que el código consume:

```
company_positions.icon         | 0
seniority_levels.years_range   | 0
company_culture_values.icon    | 0
company_benefits.icon          | 0
tech_stack.abbreviation        | 0
```

Todo poblado correctamente post-migraciones 012/013.

### 7. Modalidad normalizada ✅

```
offers.modality          | 'Remoto' (1), 'Presencial' (1)
offers.work_modality     | 'Remoto' (1)
profiles.modality        | 'Remoto' (2)
profiles.work_modality   | 'Híbrido' (1), 'Remoto' (1)
companies.work_model     | 'Remoto' (1), 'Presencial' (1)
```

Cero values lowercase legacy. ✅

Grep en `src/`: única ocurrencia de `'remote'` está en un **comentario explicativo** de `CreateOffer.jsx:23`. No es código. ✅

### 8. Validaciones del wrapper db.* ✅

Verificado en `supabase.js`:
- `db.matches.sendMessage`: `trim` + `length ≤ 2000` ✅
- `db.offers.create`: `title` (≤120) + `description` (≤5000) ✅
- `db.support.createTicket`: `subject` (≤200) + `message` (≤5000) ✅
- `db.storage.uploadImage` / `uploadDocument`: usan `UPLOAD_LIMITS` para tipo + size ✅

Y en `RegisterView.jsx`: regex de password con mayúscula + número/símbolo. ✅

### 9. Patrones bug-prone ✅

- `supabase.from(` directo en views: **0 matches** ✅
- `useApp().state.currentUser`: **0 matches** ✅ (post-fix #6)
- `.single()` en `supabase.js`: 12 ocurrencias — todas post-INSERT/UPDATE/upsert (no SELECT), válidas ✅

### 10. Hex hardcoded ✅

Únicas ocurrencias:
- `LoginView.jsx` / `RegisterView.jsx`: colores oficiales de Google OAuth SVG (`#4285F4`, etc.) — **excepción legítima** ✅
- `LineChart.jsx` / `BarChart.jsx`: fallbacks defensivos en `getComputedStyle().getPropertyValue() || '#fallback'` — **excepción legítima** ✅

### 11. Migraciones aplicadas vs versionadas ⚠️

- **Local `sql/migrations/`**: 001 a 014 + README (14 migraciones).
- **Aplicadas en Supabase**: 13 migraciones (incluye `012b_fix_positions_seniority_metadata`).

**Drift menor**: la migración `012b_fix_positions_seniority_metadata` se aplicó en Supabase pero NO existe como archivo local separado. Sus UPDATEs fueron consolidados manualmente dentro del archivo local `012_company_positions_seniority_metadata.sql`. El efecto neto en BD es equivalente, pero el historial no es 1:1.

**Recomendación**: para reproducibilidad pura, valdría la pena exportar `012b` desde Supabase y guardarlo como `sql/migrations/012b_fix_positions_seniority_metadata.sql`, o bien aceptar el drift documentado en README.

### 12. Routing Guards ✅

Verificado en `App.jsx`:
- Línea 75: `<Route element={<PrivateRoute />}>` envolviendo todas las rutas no públicas. ✅
- Línea 85: `<Route element={<OnboardingGate />}>` envolviendo `/app/*` y `/company/*`. ✅
- Líneas 81-82: `/onboarding/candidate` y `/onboarding/company` están dentro de PrivateRoute pero **fuera del OnboardingGate** (evita redirect loop). ✅
- `/login`, `/register`, `/recovery`, `/auth/callback`, `/terms`, `/privacy`, `/faq`, `/support` están fuera de PrivateRoute. ✅

Componentes verificados:
- `OnboardingGate.jsx:29`: chequea `!profile || !profile.onboarding_completed` ✅
- `RoleRedirect.jsx:20`: chequea idem ✅
- `LoginView.jsx:88`: chequea `!profileData || !profileData.onboarding_completed` ✅

### 13. Business Rules estáticas ⚠️

| ID | Regla | Status |
|---|---|---|
| BR-S01 | Mensajes validan length ≤ 2000 | ✅ PASS |
| BR-S02 | Offers validan title/description | ✅ PASS |
| BR-S03 | Privacy queries usan PUBLIC_COLS | ✅ PASS |
| BR-S04 | Password complejidad | ✅ PASS |
| BR-S05 | Storage upload valida tipo+size | ✅ PASS |
| BR-S06 | RPC delete_account existe y se usa | ✅ PASS |
| BR-S07 | NotificationsView usa useAuth() | ✅ PASS |
| BR-S08 | `.single()` solo post-INSERT/UPDATE | ✅ PASS |
| BR-S09 | Realtime con cleanup | ✅ PASS |
| BR-S10 | Modalidades capitalizado | ✅ PASS |

**Subwarning BR-S** detectado fuera de la lista oficial:

🟡 **`useMessages.js:16,33` early return sin `setLoading(false)`**. Si `matchId` es null al mount, `loading` queda en `true` infinitamente. En la práctica `useMessages` solo se llama desde rutas con `:matchId` así que NO se reproduce, pero es defensive coding fallido según Section 12.6 del agente.

Igual en `useOnboardingCandidate.js:69,106` y `useOnboardingCompany.js:73,103`: `if (!user) return` sin `setLoading(false)`. Mismo argumento — protegido por OnboardingGate, pero patrón frágil.

**Severidad**: 🟡 Bajo. No reproduce en flujos actuales, pero conviene reforzar para evitar regresiones futuras.

---

## ⚠️ Hallazgos (resumen)

### 🟡 Medio (no bloqueantes)

1. **Migración 012b drift** — aplicada en Supabase, no versionada localmente. Exportar para reproducibilidad.
2. **Hooks con early return sin setLoading(false)** — `useMessages`, `useOnboardingCandidate`, `useOnboardingCompany`. En práctica no reproduce porque los gates de routing los protegen, pero conviene cerrar el pattern.

### 🔴 Crítico

Ninguno. ✅

---

## Comparación con audits previos

| Fecha | Score | Notas |
|---|---|---|
| 2026-05-18 v1 (10 secciones) | 7.0/10 | Reporte inicial. 5 fixes pendientes. |
| 2026-05-18 v2 (re-corrida) | 8.5/10 | 4 bugs críticos detectados en runtime, fixed. |
| **2026-05-19 v3 (13 secciones)** | **11/13 + 2 ⚠️** | Sin críticos. Las 2 secciones nuevas (Routing Guards, Business Rules) confirman que los fixes recientes están en su lugar. |

## Lo que NO cubre este reporte

(Pendiente para qa-e2e en runtime):
- **Suite 0 Fresh User Journey** — flujo completo de un user desde cero (signup → onboarding → app)
- **Suite 2 Path Coverage Matrix** — cada URL × cada estado auth
- Verificación visual (dark mode, charts, responsive)
- Smoke tests de realtime con 2 sessions

Para correr el qa-e2e refactorizado, conectar Claude Preview MCP, levantar `npm run dev`, y pedir "corre qa-e2e".

## Acciones recomendadas

1. **Cerrar pattern de early return** en hooks (1-line fix x 3 archivos): agregar `setLoading(false)` antes del `return` en los hooks listados.
2. **Exportar 012b** desde Supabase o aceptar el drift documentado en `sql/migrations/README.md`.
3. **Próximo paso**: correr qa-e2e v2 (refactorizado) para validar Fresh Journey + Path Coverage Matrix.
