# QA E2E v3 — 2026-05-18 (post-fixes)

**Agente**: qa-e2e (Claude Preview MCP)
**Cobertura**: 100% de los fixes verificados en runtime.
**Tiempo de ejecución**: ~10 min.

## Fixes aplicados y verificados

| # | Bug | Fix | Estado |
|---|---|---|---|
| **#3** | WelcomeView dark mode invisible | Nuevo token `--text-on-dark: #FFFFFF` (no se invierte) + reemplazo de `--text-inverse` por `--text-on-dark` en WelcomeView.css | ✅ Verificado: `getComputedStyle(.welcome-brand).color = rgb(255,255,255)` en dark |
| **#4** | Chat: mensajes duplicados | `useMessages.js` reconcilia optimistic + realtime echo: si llega un INSERT con mismo content+sender_id que un optimistic pendiente, lo reemplaza en lugar de agregar | ✅ Verificado: enviar mensaje aparece 1 vez (antes 2) |
| **#5** | Mensaje largo sin feedback visible | `useMessages.sendMessage` retorna `{ error }` legible; `Chat.jsx` muestra `<div className="chat-view__send-error">` con icono + texto del error; restaura `inputText` para que el usuario edite; auto-clear en 5s | ✅ Verificado: aparece "El mensaje no puede superar 2000 caracteres" inline |
| **#6** | NotificationsView infinite loading | Cambiar `useApp().state.currentUser` → `useAuth().user` en NotificationsView.jsx y CompanyNotificationsView.jsx | ✅ Verificado: notifs del seed se renderizan |
| **#7** | AuthContext lock AbortError | Doble fix: (1) `createClient` con `auth.lock: noopLock` deshabilita el LockManager de Supabase JS que choca con React StrictMode; (2) AuthContext usa solo `onAuthStateChange` (sin `getSession`/`getUser` explícito) — el evento INITIAL_SESSION hidrata user al subscribir | ✅ Verificado: NotificationsView, CompanyStats y CreateOffer cargan correctamente |

## Suites E2E completas

| Suite | Estado | Notas |
|---|---|---|
| 1. Auth (login + register validaciones) | ✅ PASS | Credenciales OK, validaciones funcionando |
| 2. Welcome dark mode | ✅ PASS | Texto blanco visible en dark |
| 3. Swipe candidato (empty state) | ✅ PASS | Empty state correcto |
| 4. MatchesView | ✅ PASS | Match del seed visible con story circle |
| 5. Chat (mensajes) | ✅ PASS | 5 históricos + envío + validaciones |
| 6. NotificationsView candidato | ✅ PASS | 2 notifs del seed agrupadas por "HOY" |
| 7. CompanyDashboard | ✅ PASS | KPIs reales (1 oferta, 1 match, 8 vistas) |
| 8. CompanyStats | ✅ **PASS** | Charts + funnel + KPIs cargan con datos. **Antes bloqueada por #7.** |
| 9. CreateOffer | ✅ **PASS** | Wizard 4 pasos accesible. **Antes bloqueada por #7.** |
| 10. Privacy network check | ✅ PASS | `getDiscovery` sin email/birthday/coords |

**Score**: 10/10 ✅

## Cambios técnicos

### `Talently_v2/src/lib/supabase.js`
```js
const noopLock = async (_name, _acquireTimeout, fn) => await fn();

export const supabase = createClient(SUPABASE_URL, SUPABASE_ANON_KEY, {
    auth: {
        storage: typeof window !== 'undefined' ? window.localStorage : undefined,
        persistSession: true,
        autoRefreshToken: true,
        detectSessionInUrl: true,
        lock: noopLock,
    },
});
```

### `Talently_v2/src/context/AuthContext.jsx`
Eliminada `initSession()` con `getUser()`/`getSession()`. Ahora solo `onAuthStateChange` con `INITIAL_SESSION` event:

```js
useEffect(() => {
    let isMounted = true;
    const { data: { subscription } } = supabase.auth.onAuthStateChange(
        async (event, session) => {
            if (!isMounted) return;
            const newUser = session?.user ?? null;
            setUser(newUser);
            if (newUser) {
                const { data: profileData } = await db.profiles.getById(newUser.id);
                if (!isMounted) return;
                setProfile(profileData);
            } else {
                setProfile(null);
            }
            if (isMounted) setLoading(false);
        }
    );
    return () => { isMounted = false; subscription.unsubscribe(); };
}, []);
```

### `Talently_v2/src/views/candidate/NotificationsView.jsx` y `Talently_v2/src/views/company/CompanyNotificationsView.jsx`
```diff
- import { useApp } from '../../context/AppContext';
+ import { useAuth } from '../../context/AuthContext';
- const { state } = useApp();
- const userId = state.currentUser?.id;
+ const { user } = useAuth();
+ const userId = user?.id;
```

### `Talently_v2/src/hooks/useMessages.js`
- Realtime callback ahora reconcilia: si llega un echo del propio sender con mismo content, reemplaza el optimistic (no agrega duplicado).
- `sendMessage` ahora retorna `{ error }` legible para que la vista pueda mostrar feedback.

### `Talently_v2/src/views/candidate/Chat.jsx`
- Nuevo `useState(sendError)`.
- `handleSend` captura el resultado de `sendMessage` y setea el error.
- JSX: nuevo `<div className="chat-view__send-error">` con icono + texto.
- Input: `maxLength={2000}` para bloqueo client-side adicional.

### `Talently_v2/src/views/candidate/Chat.css`
Estilo del error inline con fondo `rgba(--danger-rgb, 0.1)`, color `--danger`, icono + texto.

### `Talently_v2/src/styles/variables.css`
Nuevo token `--text-on-dark: #FFFFFF` solo en `:root` (NO sobreescrito en `[data-theme='dark']`).

### `Talently_v2/src/views/public/WelcomeView.css`
Replace global `var(--text-inverse)` → `var(--text-on-dark)`.

## Backlog menor (sin fix en esta sesión)

- 🟡 `console.error('[useMessages] Error...', error)`: ahora muestra el `error.message` correctamente (`"El mensaje no puede superar 2000 caracteres"` en vez de `[object Object]`). PASS.
- 🟡 Lock no-op deshabilita sync multi-tab. Si en el futuro hay 2 tabs abiertas, una puede refrescar token mientras la otra lo usa stale. Aceptable para una app móvil-first donde el usuario rara vez abre 2 tabs.
- 🟡 Cuando se logout y vuelva a hacer login, hay un loading breve de ~500ms antes que cargue. Debido a que `onAuthStateChange` corre `db.profiles.getById` async — no es bloqueante de UX, solo perceptible.

## Conclusión

Todos los 5 fixes verificados con runtime tests + screenshots/snapshots/computed-styles. El stack de Talently_v2 está en **condición sólida** para QA visual manual y dogfooding interno.

Próximos pasos recomendados:
1. QA visual manual con el checklist de [docs/qa/2026-05-18-checklist.md](2026-05-18-checklist.md) — ahora todas las vistas son accesibles.
2. Crear un script de seed automatizado (Node) para evitar el paso manual del dashboard de Supabase en próximas corridas.
3. Considerar agregar tests de integración con Vitest para cubrir el wrapper `db.*`.
