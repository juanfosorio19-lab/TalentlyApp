# Dependencias
node_modules/

# Build outputs
dist/
dist-ssr/
build/

# Logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Variables de entorno
.env
.env.*
!.env.example
*.local

# Editor
.vscode/*
!.vscode/extensions.json
.idea/
.DS_Store
*.suo
*.swp

# Claude Code (worktrees locales y settings de maquina)
.claude/worktrees/
.claude/settings.local.json
.claude/agent-memory/
.claude/launch.json

# Artefactos de Windows
nul
Thumbs.db

# Scripts locales de inspeccion
test.ps1

# ── Capacitor / Android ──
# Claves de firma — NUNCA versionar (perderlas = no poder actualizar la app en Play)
*.keystore
*.jks
keystore.properties
# Artefactos de build Android (por si el android/.gitignore no aplica)
Talently_v2/android/.gradle/
Talently_v2/android/build/
Talently_v2/android/app/build/
Talently_v2/android/local.properties
Talently_v2/android/app/release/
# Capacitor copia el build aquí en cada sync
Talently_v2/android/app/src/main/assets/public/
# Recursos fuente de iconos (opcional versionar — pesados)
Talently_v2/resources/
